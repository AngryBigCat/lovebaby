name: lovebaby

on:
  push:
  pull_request:
  watch:
    types: [ started ]
  schedule:
    - cron: 5 * * * *

jobs:
  GLaDOS-checkin:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: lovebaby
        run: |
          ./run.sh
